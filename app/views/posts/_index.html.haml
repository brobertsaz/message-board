- @posts.order(created: :desc).each do |post|
  %tr
    %td
      = link_to post.title, post_path(post)
    %td
      = post.body
    %td
      = post.user.full_name
      //= link_to 'Edit', edit_post_path(post), remote: true, class: 'btn btn-default'
      //= link_to 'Delete', post_path(post), method: :delete, remote: true, class: 'btn btn-danger'
