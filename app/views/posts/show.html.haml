%h1
  = @post.title
%h3
  = @post.body
%ul
  - @post.comments.order(:created_at).each do |cmt|
    %li= cmt.content

= form_for(Comment.new, url: comments_path) do |f|
  = f.text_field :content
  = f.hidden_field :post_id, value: @post.id
  = f.submit "Create", class: 'btn btn-primary'

